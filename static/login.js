const form=document.querySelector('#loginForm');const button=document.querySelector('#loginButton');const statusBox=document.querySelector('#formStatus');form.addEventListener('submit',async event=>{event.preventDefault();statusBox.textContent='';button.disabled=true;button.querySelector('span').textContent='正在登录…';try{const response=await fetch('/api/auth/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({username:form.username.value.trim(),password:form.password.value})});const data=await response.json();if(!response.ok){statusBox.textContent=data.error==='TOO_MANY_ATTEMPTS'?'尝试次数过多，请 15 分钟后再试。':'用户名或密码不正确。';return}window.location.replace('/')}catch(error){statusBox.textContent='暂时无法连接服务器，请稍后重试。'}finally{button.disabled=false;button.querySelector('span').textContent='登录工作台'}});
